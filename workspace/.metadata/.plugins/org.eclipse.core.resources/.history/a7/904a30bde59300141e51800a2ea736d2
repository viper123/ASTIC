package ro.info.asticlib.db;

public enum Triggers {
	public FILE_TAGGING_LOGGER("CREATE OR REPLACE TRIGGER file_taging_logger"+
			"BEFORE INSERT"+
			"ON BW"+
			"FOR EACH ROW"+
			"DECLARE"+
			"BEGIN"+
			"insert into FILE_JURNAL(file_path,tag_extraction) values(:new.file_path,sysdate);"+
			"END;");
	
	private Triggers(String creationSQL){
		this.creationSQL = creationSQL;
	}
	
	private String creationSQL ;
}
